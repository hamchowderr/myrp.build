-- Test fixture only -- NOT the real ox_lib and contains no ox source code.
-- A minimal manifest so the agent can detect ox_lib as an installed sibling
-- resource during cross-resource read tests. The real ox_lib is authored by
-- Overextended and installed separately by the user under its own license.
fx_version 'cerulean'
game 'gta5'

name 'ox_lib'
author 'myRP.build (test fixture stub)'
description 'Stub manifest so the agent can detect ox_lib as an installed sibling resource. Not the real ox_lib.'
